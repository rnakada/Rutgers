
var Student = function(name, subject, gpa){
    // * Name of the student
    this.name = name;
    // * Favorite subject
    this.subject = subject;
    // * Current GPA
    this.gpa = gpa;
  
};

module.exports = Student;